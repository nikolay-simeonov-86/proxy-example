{
  "name": "proxy-example",
  "version": "1.0.0",
  "main": "index.js",
  "description": "A proxy example with three routes",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "scripts": {
    "start": "node -r dotenv/config index.js dotenv_config_path=.env dotenv_config_debug=false",
    "dev": "npx nodemon -r dotenv/config index.js dotenv_config_path=.env dotenv_config_debug=true"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
